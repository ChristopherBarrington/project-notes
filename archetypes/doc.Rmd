---
title: {{ default (.File.Path | path.Dir | path.Base | replaceRE "^20\\d+-\\d+-\\d+-" "" | humanize | title) (getenv "TITLE") }}
weight: 1

categories:

tags:

authors:
  - christopher-barrington

type: docs
toc: true
draft: false

date: `r format(Sys.time(), "%Y-%m-%d")`
created: {{ now.Format "2006-01-02" }}

always_allow_html: true
output:
  html_document:
    keep_md: yes
    df_print: paged
---

<!-- initial set up chunks are invisible -->

<!-- make subdirectories to write into/any bash work -->
```{bash setup_wd, echo=FALSE, cache=FALSE, code=readLines(system.file('chunks/working_directory.sh', package='project.notes.tools'))}
```

<!-- read the standard knitr setup script -->
```{r knitr_setup, cache=FALSE, echo=FALSE, message=FALSE, warning=FALSE, code=readLines(system.file('chunks/knitr.r', package='project.notes.tools'))}
```

<!-- load any packages for the workbook -->
```{r load_workbook_packages, cache=FALSE, echo=FALSE}
```

<!-- read the standard setup script, loading well-used libraries -->
```{r configure_r, cache=FALSE, echo=FALSE, code=readLines(system.file('chunks/environment.r', package='project.notes.tools'))}
```

<!-- DOIs for software used in the workbook or to generate presented results, a named list of strings -->
```{r add_software_to_bibliography, cache=FALSE, echo=FALSE}
c() -> academic_doi
c() -> software_doi
```

<!-- write a bibliography (test) -->
```{r write_bibliography, cache=FALSE, echo=FALSE, code=readLines(system.file('chunks/write_bibliography.r', package='project.notes.tools')), eval=FALSE}
```

<!-- set a ggplot theme to use as a base in the workbook -->
```{r define_global_ggplot_theme, cache=FALSE, echo=FALSE}
(theme_bw() +
 theme(text=element_text(size=10, colour='black'),
       panel.grid.minor=element_blank(),
       strip.background=element_blank())) %>%
theme_set()
```

<!-- any final things to set for this workbook -->
```{r final_setup, cache=FALSE, echo=FALSE}
```

<!-- define slugs for other reports here for ease and reuse -->
```{r define_slugs, cache=FALSE, echo=FALSE}
slugs <- list()
```

<!-- hyperlinks: [Crick website][CRICK] -->
[CRICK]: https://www.crick.ac.uk

<!-- end of initial set up chunks -->

Anything here is included in the summary.

<!--more-->

















<!-- include a session information section -->

```{r record_saved_objects, cache=FALSE, echo=FALSE}
#! names of object to be included in the table of hashes
saved_objects <- c()
```

```{r show_session_information, cache=FALSE, child=system.file('chunks/session_information.Rmd', package='project.notes.tools')}
```

<!-- end of session information -->

